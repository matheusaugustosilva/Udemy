//CÓDIGO NÃO EXECUTÁVEL

//Procedural
processamento(valor1, valor2, valor3)

//OO
objeto = { // Esse meu objeto tem dados e funções 
    valor1, 
    valor2, 
    valor3, 
    processamento(){

    }
}
objeto.processamento()  // O processamento está dentro do objeto('Objeto') que utiliza os dados que estão armazenados nele
                        // Foco passou a ser o objeto

// 1. Abstração: Focar no essencial e trazer objetos do mundo real para dentro do software
/* 2. Encapsulamento: Esconder os detalhes de implementação, mas expor uma interface comunicação
EX: Sei que preciso de combustível, mas não como ele é consumido*/ 
// 3. Herança(prototype): Recebe atributos de um pai, js possui herança única
// 4. Polimorfismo: Multipla formas  